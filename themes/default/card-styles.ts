/**
 * Default theme card styling
 * Uses standard shadcn card appearance
 */

export const defaultCardClasses = {
    // Standard shadcn card styling
    base: 'bg-card',
    hover: '', // No special hover effect
};
